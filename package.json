{
  "name": "brandkit-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "node scripts/build-llms-full.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:llms": "node scripts/build-llms-full.mjs",
    "test": "npm run docs:build && node scripts/check-docs-dist.mjs"
  },
  "devDependencies": {
    "markdown-it-task-lists": "^2.1.1",
    "vitepress": "^1.6.4"
  },
  "overrides": {
    "esbuild": "^0.25.12",
    "vite": "^6.4.3"
  }
}
